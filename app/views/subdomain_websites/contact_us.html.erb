<div id="contactus-content-container">
	
	<p class="lead">Fale Conosco</p>
	<p>Fique a vontade para falar conosco através do formulário abaixo. Iremos responder o mais rápido possível.</p>
	
	<%= form_tag(subdomain_websites_contact_us_path) do -%>
		
		<%= hidden_field_tag :contact_email_to, @website.agency.email %>
		
		<div class="contact-input-field">
			<%= label_tag :contact_name, "Nome:*", :required => true %>
			<%= text_field_tag :contact_name, nil, :required => true, :autofocus => true %>
		</div>
		<div class="contact-input-field">
			<%= label_tag :contact_mail, "Email:*" %>
			<%= text_field_tag :contact_mail, nil, :required => true %>
		</div>
		<div class="contact-input-field">
			<%= label_tag :contact_msg, "Mensagem:*" %>
			<%= text_area_tag :contact_msg, nil, :required => true %>
		</div>
		
		<div><%= submit_tag 'Enviar', :class => "btn-silver-light-fluid" %></div>
		
	<% end -%>
	
</div>
